Data underlying Supp. Fig. 1 -- comparison of the AlphaFold2 structures generated by the lightweight versions that run via Jupyter Notebooks (e.g., ColabFold) vs. the AlphaFold Protein Structure Database.

pdb.py is located in each of the three sub-directories. This python script searches for all .pdb files in a directory, extracts the pLDDT scores for each residue, and makes a correlation plot
